<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DocumentEditor"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".ui.activities.setPassword.SetPasswordActivity"
            android:exported="false"
            android:label="@string/title_activity_document_set_password" />
        <activity
            android:name=".ui.activities.documentId.DocumentIdActivity"
            android:exported="false"
            android:label="@string/title_activity_document_id"
            android:theme="@style/Theme.DocumentEditor.NoActionBar" />
        <activity
            android:name=".ui.activities.newDocument.NewDocumentActivity"
            android:exported="false"
            android:label="@string/title_activity_new_document" />
        <activity
            android:name=".ui.activities.userInfoId.UserInfoIdActivity"
            android:exported="false"
            android:label="@string/title_activity_user_info_id" />
        <activity
            android:name=".ui.activities.auth.AuthActivity"
            android:exported="false"
            android:label="@string/title_activity_auth" />
        <activity
            android:name=".ui.activities.main.MainActivity"
            android:exported="false"
            android:theme="@style/Theme.DocumentEditor.NoActionBar" />
        <activity
            android:name=".ui.activities.welcome.WelcomeActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>